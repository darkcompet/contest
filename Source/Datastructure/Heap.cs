namespace Compet.Datastructure;

/// <summary>
/// This is heap, or priority queue as another name.
/// It performs some fast actions:
/// - FindMin()/FindMax(): O(1)
/// - Enqueue()/Dequeue(): O(log(N))
/// </summary>
/// <typeparam name="TElement"></typeparam>
/// <typeparam name="TPriority"></typeparam>
public class Heap<TElement, TPriority> : PriorityQueue<TElement, TPriority> {
	//
}
